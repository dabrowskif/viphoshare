import { makeStyles } from '@mui/styles';

export default makeStyles(() => ({
  mainGrid: {
    width: '100%',
    position: 'relative',
    justifyContent: 'center',
    textAlign: 'center',
    marginTop: '100px',
  },
}));
